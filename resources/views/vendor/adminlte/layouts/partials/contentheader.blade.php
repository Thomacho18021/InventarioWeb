<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        @yield('contentheader_title', 'Titulo de la pagina')
        <small>@yield('contentheader_description')</small>
    </h1>
    @yield('menuderecho')
</section>